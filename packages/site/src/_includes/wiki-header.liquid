<h1>{{ lexicon.title }}</h1>

{% if lexicon.group or lexicon.metadata %}
	<div class='item-header'>
		<div class='item-metadata'>
			{% for meta in lexicon.metadata %}
				{% if meta.value %}
					{% if meta.slug %}
						<span class='{{ meta.cssClass }}'>{{ meta.title }}: <a href="/wiki/{{ meta.slug }}">{{ meta.value }}</a></span>
					{% else %}
						<span class='{{ meta.cssClass }}'>{{ meta.title }}: {{ meta.value }}</span>
					{% endif %}
				{% else %}
					<span class='{{ meta.cssClass }}'>
						<a href='/wiki/{{ meta.key }}/'>{{ meta.title }}</a>
					</span>
				{% endif %}
			{% endfor %}
		</div>

		<div class='breadcrumb'>
			{% if lexicon.group %}
				<a href='/wiki/{{ lexicon.groupSlug }}/'>← Back to {{ lexicon.group | replace: '_', ' ' }}</a>
			{% endif %}
		</div>
	</div>
{% endif %}
