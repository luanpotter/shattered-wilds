import { MODULE_ID } from '../consts';

export const SelectTool = {
	toggle: (active: boolean): void => {
		// TODO: not implemented yet
		console.log(`[${MODULE_ID}] toggling select tool [WIP]`, { active });
	},
};
